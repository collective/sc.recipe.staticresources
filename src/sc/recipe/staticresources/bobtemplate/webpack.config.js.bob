const makeConfig = require('sc-recipe-staticresources');


module.exports = makeConfig(
  // name
  '{{{name}}}',

  // shortName
  '{{{short_name}}}',

  // path
  `${__dirname}/{{{destination}}}`,
  // `${__dirname}/../src/{{{name.replace('.', '/')}}}/browser/static`,
  // `${__dirname}/../src/{{{name.replace('.', '/')}}}/theme`,

  //publicPath
  '',
  // '++resource++{{{name}}}/',
  // '++theme++{{{name}}}/',

  //callback
  (config, options) => {
  },
);
